# Parser Maps Pro

Профессиональный парсер организаций с Яндекс.Карт с поддержкой SQLite и PostgreSQL.

## 📋 Описание

Parser Maps Pro - это мощный инструмент для автоматического сбора информации об организациях с Яндекс.Карт. Проект предоставляет веб-интерфейс для управления задачами парсинга и экспорта результатов в Excel.

### Основные возможности

- 🗺️ Парсинг организаций по поисковым запросам
- 📂 Парсинг по категориям Яндекс.Карт
- 🔍 MapScan режим для глубокого сканирования карты
- 💾 Поддержка SQLite и PostgreSQL
- 🌐 Веб-интерфейс для управления
- 📊 Экспорт результатов в Excel
- ⚡ Многопоточная обработка
- 🔄 Система retry и обработка капчи
- 🎯 Фильтрация по исключениям

## 🚀 Быстрый старт

### Требования

- Python 3.8+
- Google Chrome или Chromium
- ChromeDriver (прилагается в проекте)

### Установка

1. Клонируйте репозиторий:
\\\ash
git clone https://github.com/klimgaranin/parser_maps_pro.git
cd parser_maps_pro
\\\

2. Создайте виртуальное окружение:
\\\ash
python -m venv .venv
.venv\\Scripts\\activate  # Windows
source .venv/bin/activate  # Linux/Mac
\\\

3. Установите зависимости:
\\\ash
pip install -r requirements.txt
\\\

4. Создайте файл .env на основе .env.example:
\\\ash
cp .env.example .env
\\\

5. Отредактируйте .env и установите пароль:
\\\nv
ADMIN_USER=admin
ADMIN_PASS=ваш_надёжный_пароль
\\\

### Запуск

\\\ash
python run_ui.py
\\\

Откройте браузер и перейдите по адресу: http://localhost:8787

## 📖 Использование

### Структура config.xlsx

Создайте Excel-файл с 4 листами:

#### 1. Cities (Города)
| manager | region | city |
|---------|--------|------|
| Иванов И.И. | Московская область | Москва |
| Петров П.П. | Санкт-Петербург | Санкт-Петербург |

#### 2. Requests (Поисковые запросы)
| query_ru |
|----------|
| стоматология |
| кафе |
| автосервис |

#### 3. Categories (Категории Яндекс.Карт)
| category_path | enabled |
|---------------|---------|
| shops/building | 1 |
| food/cafe | 1 |
| auto/service | 0 |

Путь категории берётся из URL Яндекс.Карт. Например:
- https://yandex.ru/maps/moscow/category/cafe → \ood/cafe\
- https://yandex.ru/maps/moscow/category/building_shop → \shops/building\

#### 4. Excludes (Исключения)
| text |
|------|
| закрыто |
| не работает |
| временно недоступно |

### Режимы работы

- **LINKS_SEARCH** - поиск по запросу
- **LINKS_CATEGORY** - поиск по категории
- **LINKS_MAPSCAN_SEARCH** - поиск с MapScan (глубокое сканирование карты)
- **LINKS_MAPSCAN_CATEGORY** - категория с MapScan

### Процесс работы

1. Загрузите config.xlsx через веб-интерфейс
2. Нажмите "Старт" для начала парсинга
3. Следите за прогрессом в реальном времени
4. Экспортируйте результаты в Excel

### Обработка капчи

При появлении капчи:
1. Статус задачи изменится на WAITCAPTCHA
2. Откройте Chrome-профиль в папке output/chrome_profile_*
3. Решите капчу вручную
4. Нажмите "Retry" в интерфейсе

## ⚙️ Конфигурация

### Переменные окружения (.env)

\\\nv
# Сервер
APP_HOST=0.0.0.0
APP_PORT=8787

# Аутентификация
ADMIN_USER=admin
ADMIN_PASS=ваш_пароль

# База данных
DB_KIND=sqlite                              # sqlite или postgres
DB_PATH=./output/progress.sqlite           # для SQLite
DB_DSN=postgresql://user:pass@host/db      # для PostgreSQL

# Экспорт
EXPORT_XLSX=./output/results.xlsx
EXPORT_SHEET=Results

# Selenium
CHROME_BINARY=                              # путь к Chrome (опционально)
HEADLESS=0                                  # 0 = с UI, 1 = без UI

# Производительность
LINKS_WORKERS=1                             # потоков для сбора ссылок
INFO_WORKERS=1                              # потоков для сбора информации

# Отладка
SAVE_DEBUG_HTML=1                           # сохранять HTML страниц
DEBUG_DIR=./debug

# MapScan
MAPSCAN_ALWAYS=1                            # всегда использовать MapScan
MAPSCAN_GRID=3                              # сетка 3x3
MAPSCAN_DRAG_PX=420                         # сдвиг карты в пикселях
\\\

## 🗂️ Структура проекта

\\\
parser_maps_pro/
├── app/                    # FastAPI приложение
│   ├── main.py            # Основной роутер и UI
│   └── auth.py            # Базовая аутентификация
├── core/                   # Ядро системы
│   ├── config_loader.py   # Загрузка config.xlsx
│   ├── db_factory.py      # Фабрика БД
│   ├── db_sqlite.py       # SQLite драйвер
│   ├── db_pg.py           # PostgreSQL драйвер
│   ├── pipeline.py        # Конвейер обработки
│   ├── export.py          # Экспорт в Excel
│   └── utils.py           # Утилиты
├── providers/              # Провайдеры карт
│   └── yandex/            # Яндекс.Карты
│       ├── driver.py      # Selenium драйвер
│       ├── link_collector.py  # Сбор ссылок
│       ├── info_collector.py  # Сбор информации
│       └── soup_extract.py    # Парсинг HTML
├── data/                   # Данные пользователя
│   └── config.xlsx        # Конфигурация задач
├── output/                 # Результаты
│   ├── progress.sqlite    # БД прогресса
│   └── results.xlsx       # Экспортированные данные
├── runtime/                # Рабочие файлы
│   └── debug/             # HTML для отладки
├── docs/                   # Документация
├── .env                    # Переменные окружения
├── .env.example           # Пример .env
├── requirements.txt       # Зависимости
└── run_ui.py              # Точка входа
\\\

## 🔧 Разработка

### Добавление нового провайдера (например, 2GIS)

1. Создайте папку \providers/gis2/\
2. Реализуйте функции:
   - \collect_task_links(driver, task)\ - сбор ссылок
   - \parse_org_page(driver, url)\ - парсинг организации
3. Подключите в \pp/main.py\

### База данных

#### Таблицы:

- **tasks** - задачи парсинга
- **links** - собранные ссылки организаций
- **orgs** - информация об организациях
- **templates** - шаблоны экспорта

#### Статусы задач:

- PENDING - ожидает
- RUNNING - выполняется
- DONE - завершена
- ERROR - ошибка
- WAITCAPTCHA - требуется решить капчу

## 📊 API

### Эндпоинты

- \POST /api/upload_config\ - загрузить config.xlsx
- \POST /api/start\ - запустить парсинг
- \POST /api/stop\ - остановить парсинг
- \GET /api/status\ - статус системы
- \GET /api/tasks\ - список задач
- \POST /api/task_retry\ - повторить задачу
- \POST /api/task_requeue\ - вернуть задачу в очередь
- \POST /api/export\ - экспорт в Excel
- \GET /api/download\ - скачать файл

## ⚠️ Важные замечания

1. **Капча**: Яндекс может показывать капчу при интенсивном парсинге
2. **Скорость**: Рекомендуется использовать 1 воркер для избежания блокировок
3. **Chrome Profile**: Каждый воркер создаёт свой профиль Chrome
4. **Прокси**: Для масштабного парсинга используйте прокси (требует доработки)

## 🛣️ Roadmap

- [ ] Поддержка 2GIS
- [ ] Поддержка прокси
- [ ] Планировщик задач
- [ ] Telegram уведомления
- [ ] Docker контейнер
- [ ] API для интеграции
- [ ] Дедупликация результатов
- [ ] Геокодирование адресов

## 📝 Лицензия

MIT License - свободное использование в коммерческих и некоммерческих проектах.

## 🤝 Вклад в проект

Приветствуются Pull Request'ы! Перед отправкой убедитесь, что:
- Код соответствует PEP 8
- Добавлены docstrings
- Проект запускается без ошибок

## 📧 Контакты

GitHub: https://github.com/klimgaranin/parser_maps_pro
Issues: https://github.com/klimgaranin/parser_maps_pro/issues

---

**Сделано с ❤️ для автоматизации сбора данных**
